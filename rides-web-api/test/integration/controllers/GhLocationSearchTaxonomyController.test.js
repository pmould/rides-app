'use strict'

const assert = require('assert')

describe('GhLocationSearchTaxonomyController', () => {
  it('should exist', () => {
    assert(global.app.controllers.GhLocationSearchTaxonomyController)
  })
})
