'use strict'

const assert = require('assert')

describe('Gh_location_search_taxonomyController', () => {
  it('should exist', () => {
    assert(global.app.controllers.Gh_location_search_taxonomyController)
  })
})
