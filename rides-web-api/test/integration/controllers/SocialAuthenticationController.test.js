'use strict'

const assert = require('assert')

describe('SocialAuthenticationController', () => {
  it('should exist', () => {
    assert(global.app.controllers.SocialAuthenticationController)
  })
})
