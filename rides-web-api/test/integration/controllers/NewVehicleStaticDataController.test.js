'use strict'

const assert = require('assert')

describe('NewVehicleStaticDataController', () => {
  it('should exist', () => {
    assert(global.app.controllers.NewVehicleStaticDataController)
  })
})
